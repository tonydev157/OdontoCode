package com.tonymen.odontocode.ui.theme

import androidx.compose.foundation.isSystemInDarkTheme
import androidx.compose.material3.*
import androidx.compose.runtime.Composable
import androidx.compose.ui.graphics.Color

// Colores del tema claro
val LightPrimary = Color(0xFF1976D2)  // Azul profundo para una apariencia limpia y profesional
val LightPrimaryVariant = Color(0xFF1565C0)  // Variante más oscura del azul claro
val LightSecondary = Color(0xFF2196F3)  // Azul claro y brillante
val LightSecondaryVariant = Color(0xFF1976D2)  // Variante más oscura del secundario
val LightBackground = Color(0xFFFFFFFF)  // Fondo blanco limpio
val LightSurface = Color(0xFFF1F8E9)  // Superficie con un verde muy suave, generando calma
val LightOnPrimary = Color(0xFFFFFFFF)  // Texto blanco sobre azul primario
val LightOnSecondary = Color(0xFF000000)  // Texto negro sobre azul secundario
val LightOnBackground = Color(0xFF000000)  // Texto negro sobre fondo claro
val LightOnSurface = Color(0xFF000000)  // Texto negro sobre superficie clara

// Nuevos colores para botones adicionales en el tema claro
val LightRegisterButtonColor = Color(0xFF64B5F6)  // Azul medio para el botón de "Crear Cuenta"
val LightOnRegisterButtonColor = Color(0xFFFFFFFF)  // Texto blanco sobre botón de "Crear Cuenta"
val LightForgotPasswordButtonColor = Color(0xFFBBDEFB)  // Azul más suave para el botón de "Olvidé mi contraseña"
val LightOnForgotPasswordButtonColor = Color(0xFF000000)  // Texto negro sobre botón de "Olvidé mi contraseña"

// Colores del tema oscuro
val DarkPrimary = Color(0xFF0D47A1)  // Azul marino oscuro
val DarkPrimaryVariant = Color(0xFF002171)  // Variante aún más oscura del azul marino
val DarkSecondary = Color(0xFF1976D2)  // Azul brillante para tema oscuro
val DarkSecondaryVariant = Color(0xFF1565C0)  // Variante más oscura del secundario
val DarkBackground = Color(0xFF121212)  // Fondo oscuro estándar
val DarkSurface = Color(0xFF1E1E1E)  // Superficie oscura, pero no totalmente negra
val DarkOnPrimary = Color(0xFFFFFFFF)  // Texto blanco sobre azul marino
val DarkOnSecondary = Color(0xFFFFFFFF)  // Texto blanco sobre secundario oscuro
val DarkOnBackground = Color(0xFFE0E0E0)  // Texto gris claro sobre fondo oscuro
val DarkOnSurface = Color(0xFFE0E0E0)  // Texto gris claro sobre superficie oscura

// Nuevos colores para botones adicionales en el tema oscuro
val DarkRegisterButtonColor = Color(0xFF1E88E5)  // Azul más oscuro para el botón de "Crear Cuenta"
val DarkOnRegisterButtonColor = Color(0xFFFFFFFF)  // Texto blanco sobre botón de "Crear Cuenta"
val DarkForgotPasswordButtonColor = Color(0xFF64B5F6)  // Azul claro para el botón de "Olvidé mi contraseña"
val DarkOnForgotPasswordButtonColor = Color(0xFFFFFFFF)  // Texto blanco sobre botón de "Olvidé mi contraseña"
