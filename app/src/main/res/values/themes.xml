<resources>
    <style name="Theme.OdontoCode" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Hacer la barra de estado transparente -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Hacer la barra de navegación transparente -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Asegurar que el contenido no se superponga a la barra de estado -->
        <item name="android:windowLightStatusBar">true</item> <!-- Si el fondo es claro -->
    </style>
</resources>
